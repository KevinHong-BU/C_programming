### 04.연습문제

#### 1. 원의 넓이를 구하는 프로그램을 매크로함수로 선언하여 작성하라.

#### 2. 매크로를 사용하여 수의 입력 범위를 지정하고 지정된 입력까지만 입력이 되도록 프로그램을 작성하라.

#### 3. 다음 매크로 선언 또는 실행문에서 어떤 문제가 있는지 설명하라.
```C
#define MAX(a, b) ((a) < (b) ? (b) : (a))
int x = 5, y = 10;
int z = MAX(x++, y++);
```

#### 4. 2차 방정식 $ax^2 +bx + c = 0$ 의 판별식 $b^2 - 4ac$가 음수이면 0을 아니면 1을 반환하는 매크로DECISION(a, b, c)을 선언하고 사용하는 예를 보여라.

#### 5. 두 변수를 교환하는 SWAP(x,y) 매크로를 선언하고 사용하는 방법을 보여라.

#### 6. 어떤 정수를 입력하여 짝수인지 홀수인지 판단하는 EVEN_ODD(x) 매크로를 작성하고 사용하는 방법을 보여라.

#### 7. 두 정수를 입력받아 같은지 다른지를 판단하는 IS_EQUAL(x,y) 매크로를 작성하고 사용 예를 보여라.

#### 8. 본인의 이름을 MY_NAME 매크로로 정의하고 사용하는 방법을 보여라.

#### 9. 본인의 생일을 MY_BIRTH 매크로에 “2010-01-01”과 같이 선언해 두고 년, 월, 일을 sscanf()함수를 이용하여 추출한 다음 “내 생일은 2020년 1월 1일”과 같이 출력하는 프로그램을 작성하라.

#### 10. 소문자를 입력하면 대문자로 바꿔주는 TOUPPER(c) 매크로를 작성하라. 단, 문자는 소문자만 입력되었다고 가정한다.

#### 11. 본문의 호텔 방 문제를 다른 방법으로 작성하라.

### 08.연습문제

#### 1.  구의 부피 구하는 함수 만들기, double SphereVolume (double);

구의 부피 = $\frac 4 3 \pi r^3$

#### 2. 최대공약수(greatest common divisor)를 이용한 최소공배수(least common multiple)를 구하는 함수 lcm을 작성하라. int gcd(int, int); int lcm(int, int);

#### 3. 어떤 정수를 넘겨주면 소수인지 판다하는 함수 isprime을 작성하라.

#### 4. 두 정수의 곱셈을 비트연산자(&, |, <<, >> 등)와 덧셈 연산만으로 계산하는 함수를 작성하라.

#### 5. 2차원 배열을 넘겨주고 그 배열의 전체 합을 구하는 함수를 작성하라.

#### 6. 대문자 <-> 소문자로 상호 변환하는 사용자 정의 함수를 작성하라.char upper2lower(char); char lower2upper(char); 

#### 7.  두 정수를 넘겨주면 두 수 사이의 합을 구하여 반환하는 함수를 작성하라.

#### 8. 문자열을 넘겨주면 거꾸로 만들어 반환하는 함수를 작성하라.

#### 9. 다음 공식을 이용하여 배열 데이터의 표준편차 $\sigma$를 구하는 함수를 작성하라. $\bar x$는 $x$의 산술평균이다. 산술평균을 구하는 함수를 따로 작성하라.
$$ \sigma = \sqrt {\frac 1 n \sum_{i=1}^{n} x_i^2 - (\bar x)^2}$$
프로그램의 흐름.
* ① main에서 배열의 데이터를 std_dev(표준편차 구하는 함수)에 전달
* ② std_dev에서 위 식의 $(\bar x)^2 = \frac 1 n \sum_{i=1}^{n} x_i )^2$ 를 먼저 계산한 다음, m_mean(산술평균 구하는 함수)으로부터 산술평균값을 return 받아 나머지 계산을 한다.
* ③ 계산이 끝나면 main 함수로 표준편차를 return한다.

#### 10. 화씨->섭씨, 섭씨->화씨로 바꾸는 프로그램을 switch~case와 함수로 작성하라., 

#### 11. 2차 방정식 에서 a, b, c의 값을 입력받고, 허근, 중근 또는 서로 다른 두 근이 존재하는지 여부를 검사하는 함수를 작성하고, 근이 존재할 때, 근을 구하는 함수도 작성하라.근의 공식 = 

#### 12. 세수의 최대공약수를 구하는 프로그램을 작성하라.(사용자 정의 함수사용)

#### 13. n개의 원소 중에서 p개의 원소를 뽑는 방법의 수를 조합이라고 하며, 다음 식과 같이 계산할 수 있다. 함수로 작성하라.

#### 14. n개의 원소 중에서 r개를 선택해 일렬로 세우는 방법 수인 순열은 다음 공식을 따른다. 함수로 작성하라.,	

#### 15. 다음은 복리로 이자를 계산하는 방법이다. 원금 1,000,000원을 년 5.5%의 이율로 20년간 복리로 예치했을 경우 20년 뒤의 총금액은 얼마인가?c : 최초예금액, r = 이율, n = 예치기간C : 예금한 이후에 발생한 이자를 포함한 새로운 예금액(원리합계)

#### 16. 비트연산자를 이용하여 짝수를 판별하는 함수를 작성하라.

#### 17. 10진 정수를 2진수로 변환하는 함수를 본문의 내용과 다른 방법으로 작성하라.

#### 18. 임의의 2진수를 문자열로 입력받아서 10진수로 변환하는 함수를 본문과 다른 방법으로 작성하라.

#### 19. 주민등록 번호 체크 프로그램을 함수를 만들어 작성하라.(만드는 방법 설명)

#### 20. 한글이 입력될 경우 모든 공백을 지우고, 영문일 경우 앞의 공백을 지우는 프로그램을 함수로 작성하라.

#### 21. 세 자리씩 끊어서 콤마(,)삽입하는 함수를 본문과 달리 작성하라.

#### 22. 신차 구입 시 세금 계산 프로그램을 작성하라. (www.koreatax.org)에 계산법이 있다. 세 자리씩 끊어서 콤마로 자리 구분하는 함수를 이용하라.

#### 23. BMI(Body mass index)비만도 계산하여 출력하는 함수를 작성하라. (통계적으로 계산된 비만도이므로 전적으로 믿을 필요는 없음, 나라마다 또는 인종별로 다를 수 있음)

#### 24. 파스칼의 삼각형 출력하는 함수를 작성하라.

#### 25. m을 n진법으로 나타내는 함수 bas_epn(m,n)을 재귀함수로 만들고 이를 이용하여 bas_epn(324,2) 와 bas_epn(1234,16)을 구하라.

#### 26. scanf()함수를 이용하여 주민등록번호(xxxxxx-xxxxxxx)를 앞의 6자리와 뒤의 7자리를 따로 입력받아 생년, 생월, 생일을 각각 나누어 출력하고 남자인지 여자인지 구별하는 프로그램을 함수로 작성하라.

#### 27. 6장 연습문제 3번을 변경하여 본인의 생년월일을 입력하여 오늘까지 며칠을 살아 왔는지 계산하는 프로그램을 함수로 작성하라.

#### 28. 몫과 나머지를 구하는 함수를 작성하라. 몫은 함수의 결과로 받고 나머지는 포인터로 받도록 한다.

#### 29. 반올림을 구하는 함수를 작성하라. 정수값을 주고 어느 자리에서 반올림할 것인지를 넘겨주면 원하는 자리에서 반올림하여 돌려받는다.

#### 30. 요금을 지불하는 프로그램을 작성하라. 사용자는 납입금액이	1000원 미만이면 세율은 0%	1000원 이상 3000원 미만이면 세율은 15%	3000원 이상 7000원 미만이면 세율은 20%	7000원 이상 10000원 미만이면 세율은 27%	10000원 이상이면 35%의 세율을 적용한다.요금을 지불하는 방법은 1만원, 5천원, 1천원, 500원, 100원의 지폐 또는 동전이 몇 개가 사용되는지를 결정하고 100원 미만의 요금은 절삭한다. 이때 각 화폐의 종류에 따라 개수를 결정하는 함수를 작성하라. 예를 들어 사용자의 요금이 14000원이라고 입력이 되면, 요금(14000) + 세금(4900) => 총 18900원이 되고 만원 1장, 5천원 1장, 천원 3장, 500원 동전 1개, 100원 동전 4개가 필요하다고 알린다. 계산된 각 화폐의 개수는 배열을 이용하여 전달한다.

#### 31. 다음에 주어진 거리 환산식을 이용하여 거리 환산 프로그램을 함수로 각각 작성하라.	1해리(mille) = 1852m	1m = 0.5399568해리	1feet = 0.3048m	1m = 3.28084feet

#### 32. 어떤 정수의 누승을 계산하는 프로그램을 작성하라. 예를 들어 두 정수 2와 3을 입력하면 인 8이 나오게 하는 프로그램이다. 이때 main()함수에서 두수를 입력받고, 별도로 정의한 누승 계산 함수를 호출하도록 한다.

#### 33. 삼각형의 세 변을 입력받아 삼각형의 종류를 구분하는 함수 what_triangle()를 작성하라.	0. 삼각형이 아님	1. 정삼각형	2. 이등변삼각형	3. 직각 삼각형	4. 일반삼각형위의 삼각형의 종류에 따라서 0, 1, 2, 3, 4를 반환한다.

#### 34. 재귀함수를 이용하여 을 구하는 프로그램을 작성하라.

#### 35. 문자(표준ASCII 7비트)를 넘겨주고 그 문자에 대한 짝수패리티를 생성하는 프로그램을 작성하라. 단, 패리티는 ASCII코드의 MSB에 저장하고, 문자를 넘겨준 뒤 패리티를 계산하여 넘겨주는 함수와 문자를 2진수로 표현하는 함수를 각각 분리 작성하라.

#### 36. 아스키코드(8비트)를 해밍코드(12비트)로 만드는 프로그램을 작성하라. 단. 해밍코드 생성, 에러 검사, 2진수 출력 등을 함수로 만들어라.

#### 37. 단을 입력하면 그 단에 해당하는 구구단을 출력하는 함수를 작성하라.

#### 38. 문자열로 2진수 또는 그레이 코드를 읽은 후 그 반대로 바꾸는 함수, 즉, 2진 코드를 그레이 코드로, 그레이 코드를 2진 코드로 바꾸는 함수를 각각 작성하라. 또한 2진 코드를 그레이로 바꿀 것인지, 그레이 코드를 2진 코드로 바꿀 것인지 결정하기 위해 switch ~ case를 이용하여 프로그램을 작성하라.

#### 39. 카운트다운 프로그램을 재귀함수로 작성하라. 단, time()함수를 사용하여 1초 마다 1씩 카운트 다운하는 함수를 작성하고, 시작할 수를 지정한다.

#### 40. 다음과 같이 출력하는 기능을 가지는 함수를 작성하라. 이 함수는 3개의 매개변수를 가지며, 첫 번째 매개변수는 문자, 두 번째 매개변수는 한 줄에 출력될 문자의 개수, 세 번째 매개변수는 몇 줄이 출력될 것인가를 나타낸다.


#### 41. 다음 그림과 같이 숫자를 입력하면 삼각형을 출력하는 프로그램을 함수로 작성하라. <유제 05-09-1>참조.


#### 42. 부호 없는 정수(unsigned int)를 지정된 비트 수 만큼 왼쪽으로 회전하는 함수를 작성하라. 예를 들어 rotate(x, 4)를 하면 왼쪽으로 4비트 이동하고, 왼쪽에서 빠져나간 4비트는 다시 오른쪽 끝에 채워진다.

#### 43. 볼링 점수를 계산하는 프로그램을 작성하라.

#### 44. 생년월일을 입력하면 지금까지 내가 살아온 일수를 계산하는 프로그램을 작성하라. 단, 오늘의 날짜는 컴퓨터의 날짜로 한다.
